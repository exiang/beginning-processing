void setup()
{
  size(100, 100);
  background(255);
  noStroke();

  // draw the original position in gray
  fill(192, 0, 0);
  rect(0, 0, 50, 50);
  
  // todo: create another rect square at the bottom right corner using matrix

}